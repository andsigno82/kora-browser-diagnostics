{
  "name": "@kora/browser-diagnostics",
  "version": "0.1.2",
  "private": false,
  "type": "module",
  "description": "Browser Diagnostics extension for Kora Desktop",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "check": "wxt prepare && tsc --noEmit",
    "test": "vitest run --config vitest.config.ts --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wxt": "^0.20.17"
  }
}
