export default defineBackground(() => {
  // Capture and Kora bridge operations are introduced in later implementation
  // waves. Keeping the entry point intentionally passive makes the initial
  // extension package safe to build without requesting broad host access.
})

